import os

from scripts.building import *

cwd = GetCurrentDir()
src = Glob('src/*.c')
cpppath = [cwd, os.path.join(cwd, 'inc')]

group = DefineGroup('foo_hal', src, CPPPATH=cpppath)

Return('group')
